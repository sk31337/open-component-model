apiVersion: delivery.ocm.software/v1alpha1
kind: Repository
metadata:
  name: helm-argocd-crossplane-nested-repository
spec:
  repositorySpec:
    # Adjust to your OCM repository
    baseUrl: http://image-registry:5000
    type: OCIRegistry
  interval: 10m
---

apiVersion: delivery.ocm.software/v1alpha1
kind: Component
metadata:
  name: helm-argocd-crossplane-nested-component
spec:
  component: ocm.software/ocm-k8s-toolkit/examples/helm-argocd-crossplane-nested
  repositoryRef:
    name: helm-argocd-crossplane-nested-repository
  semver: 1.0.0
  interval: 10m
