apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: helm-argocd-simple-nested-status
spec:
  schema:
    apiVersion: v1alpha1
    kind: HelmArgocdSimpleNestedStatus
    spec:
      prefix: string | default="helm-argocd-simple-nested-status"
  resources:
    - id: resourceChart
      template:
        apiVersion: delivery.ocm.software/v1alpha1
        kind: Resource
        metadata:
          name: "${schema.spec.prefix}-resource-chart-name"
        spec:
          componentRef:
            name: helm-argocd-simple-nested-status-component
          resource:
            byReference:
              resource:
                name: helm-resource
          additionalStatusFields:
            oci: resource.access.toOCI()
    - id: argocdApplication
      template:
        apiVersion: argoproj.io/v1alpha1
        kind: Application
        metadata:
          name: "${schema.spec.prefix}"
          namespace: argocd
          finalizers:
            - resources-finalizer.argocd.argoproj.io
        spec:
          project: default
          source:
            chart: podinfo
            helm:
              releaseName: ${schema.spec.prefix}
            path: .
            repoURL: oci://${resourceChart.status.additional.oci.registry}/${resourceChart.status.additional.oci.repository}
            targetRevision: ${resourceChart.status.additional.oci.tag}
          destination:
            server: https://kubernetes.default.svc
            namespace: default-argocd
          syncPolicy:
            automated:
              prune: true
              selfHeal: true
            syncOptions:
              - CreateNamespace=true
