name: controller build & test

on:
  # Reusable build step for the Kubernetes controller. Invoked by the monorepo
  # shared build & publish pipeline (pipeline.yml). Runs verify-chart and build.
  # E2E moved to pipeline.yml (e2e.yml) so it runs concurrently with conformance
  # instead of serializing ahead of it. Publishing also lives in pipeline.yml so
  # that conformance can gate it.
  workflow_call:
    inputs:
      artifact_name:
        description: "The artifact name to build."
        type: string
        required: false
      ref:
        description: "The ref to build on (branch or tag). Defaults to the current ref."
        type: string
        required: false
    outputs:
      artifact_name:
        description: "The OCI image artifact name."
        value: ${{ jobs.build.outputs.artifact_name }}
      artifact_id:
        description: "The OCI image artifact id."
        value: ${{ jobs.build.outputs.artifact_id }}
      version:
        description: "Version computed for this build."
        value: ${{ jobs.build.outputs.version }}
      chart_pr_artifact:
        description: "Name of the chart artifact (without digest) used for conformance."
        value: controller-chart-pr

env:
  LOCATION: "kubernetes/controller"
  REGISTRY: ghcr.io
  REF: ${{ inputs.ref || github.head_ref || github.ref_name }}
  ARTIFACT_NAME: ${{ inputs.artifact_name || 'controller-artifacts' }}
  CHECKOUT_REPO: ${{ github.event.pull_request.head.repo.full_name || github.repository }}

permissions:
  contents: read # for actions/checkout to fetch code

# No concurrency block: this workflow is workflow_call-only. The caller
# (pipeline.yml, "monorepo shared build & publish") controls concurrency for
# the whole chain. Declaring our own would deadlock because github.workflow
# inherits the caller's name in workflow_call context.

jobs:
  verify-chart:
    name: Verify Chart
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Install Task
        uses: arduino/setup-task@b91d5d2c96a56797b48ac1e0e89220bf64044611 # v2
        with:
          version: 3.x
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Helm
        uses: azure/setup-helm@dda3372f752e03dde6b3237bc9431cdc2f7a02a2 # v5.0.0
        with:
          version: v4.2.0

      - name: Validate chart (generators + lint + template + drift check)
        working-directory: ${{ env.LOCATION }}
        run: task helm/validate

      - name: Verify no uncommitted changes
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "::error::Uncommitted changes detected after running helm tasks"
            git status && git diff
            exit 1
          fi
          echo "No uncommitted changes detected"

  build:
    name: Build Controller
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 20
    needs: verify-chart
    outputs:
      artifact_name: ${{ env.ARTIFACT_NAME }}
      artifact_id: ${{ steps.upload-artifacts.outputs.artifact-id }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Compute image refs
        env:
          OWNER: ${{ github.repository_owner }}
        run: echo "IMAGE_NAME=${REGISTRY}/${OWNER,,}/kubernetes/controller" >> "$GITHUB_ENV"

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          repository: ${{ env.CHECKOUT_REPO }}
          sparse-checkout: |
            .github/scripts
            ${{ env.LOCATION }}
            Taskfile.yml
            .env
          persist-credentials: false
          ref: ${{ env.REF }}

      - name: Setup Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        with:
          go-version-file: ${{ env.LOCATION }}/go.mod
          cache-dependency-path: ${{ env.LOCATION }}/go.sum

      - name: Install Task
        uses: arduino/setup-task@b91d5d2c96a56797b48ac1e0e89220bf64044611 # v2
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          version: 3.x

      - name: Setup docker/buildx
        uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4

      - name: Install Helm
        uses: azure/setup-helm@dda3372f752e03dde6b3237bc9431cdc2f7a02a2 # v5.0.0
        with:
          version: v4.2.0

      - name: Compute VERSION
        id: version
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
        env:
          REF: ${{ env.REF }}
          TAG_PREFIX: "v"
          # Kubernetes label values must be ≤63 chars (RFC 1123). We need to ensure the version fits within this limit
          # to be used as a label value in the Helm chart. helm.sh/chart is "<chart-name>-<version>"
          # Current chart name is "chart" = 6 chars including hyphen; 63 - 6 = 57 max version length.
          # TODO: If we change the chart name, we need to update this max version length.
          MAX_VERSION_LENGTH: "57"
        with:
          script: |
            const script = await import('${{ github.workspace }}/.github/scripts/compute-version.js');
            await script.default({ core });

      - name: Build Multi-Arch Image to OCI Layout
        working-directory: ${{ env.LOCATION }}
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          task docker-build/multi-arch \
            CONTROLLER_IMG="${IMAGE_NAME}:${VERSION}" \
            OCI_OUTPUT=tmp/oci/controller.tar

      # Package Helm chart without digest for use in PR conformance testing
      - name: Package Helm chart (without digest)
        working-directory: ${{ env.LOCATION }}
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          task helm/package VERSION="${VERSION}" APP_VERSION="${VERSION}" IMAGE_REPOSITORY="${IMAGE_NAME}"

      - name: Upload Artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        id: upload-artifacts
        with:
          if-no-files-found: error
          retention-days: 1
          name: ${{ env.ARTIFACT_NAME }}
          path: |
            ${{ env.LOCATION }}/tmp/oci/

      # Upload chart artifact separately for easier access in PR conformance testing
      - name: Upload Chart Artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          if-no-files-found: error
          retention-days: 1
          name: controller-chart-pr
          path: ${{ env.LOCATION }}/dist/chart-*.tgz
